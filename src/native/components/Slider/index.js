import React from "react";
import RCSlider from "@react-native-community/slider";

const Slider = ({...props}) => {
    return (
        <RCSlider 
            {...props}
        />
    )
}

export default Slider;